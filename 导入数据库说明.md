# 员工管理系统 - 数据库导入说明

## 一、前提条件

在导入数据库之前，请确保已安装：

1. **MySQL 8.0** 或更高版本
2. **Node.js 16.0** 或更高版本
3. **Git**（可选，用于克隆项目）

---

## 二、导入数据库步骤

### 方法1：使用命令行导入（推荐）

1. 打开命令提示符（CMD）或 PowerShell

2. 登录 MySQL：
   ```bash
   mysql -u root -p
   ```

3. 输入您的 MySQL root 密码

4. 导入数据库文件：
   ```sql
   source D:/员工管理项目/ems_database.sql
   ```
   
   **注意**：请将路径替换为实际的 `ems_database.sql` 文件路径

5. 验证导入是否成功：
   ```sql
   USE ems;
   SHOW TABLES;
   SELECT COUNT(*) FROM employees;
   ```

6. 退出 MySQL：
   ```sql
   EXIT;
   ```

---

### 方法2：使用 MySQL Workbench 导入

1. 打开 MySQL Workbench

2. 连接到本地 MySQL 服务器

3. 点击菜单：**Server** → **Data Import**

4. 选择 **Import from Self-Contained File**

5. 点击 **...** 按钮，选择 `ems_database.sql` 文件

6. 在 **Default Target Schema** 下拉框中选择 **New...**，输入 `ems`

7. 点击 **Start Import** 开始导入

8. 等待导入完成，查看日志确认无错误

---

### 方法3：使用批处理文件导入（Windows）

1. 打开 `ems_database.sql` 同目录

2. 创建一个新文件 `导入数据库.bat`，内容如下：

```batch
@echo off
chcp 65001 >nul
title 导入员工管理系统数据库
echo.
echo  ========================================
echo   正在导入数据库...
echo  ========================================
echo.

set MYSQL_USER=root
set /p MYSQL_PASSWORD=请输入MySQL密码: 
set SQL_FILE=ems_database.sql

echo.
echo 导入文件: %SQL_FILE%
echo.

mysql -u%MYSQL_USER% -p%MYSQL_PASSWORD% < "%SQL_FILE%"

if %errorlevel% equ 0 (
    echo.
    echo  ========================================
    echo   导入成功！
    echo  ========================================
) else (
    echo.
    echo  ========================================
    echo   导入失败！请检查MySQL是否已安装
    echo   以及用户名密码是否正确
    echo  ========================================
)

echo.
pause
```

3. 双击运行 `导入数据库.bat`

4. 输入您的 MySQL root 密码

---

## 三、修改数据库连接配置

导入数据库后，需要修改项目中的数据库连接信息。

### 修改 `server/server.js` 文件

找到以下代码（约第10-18行）：

```javascript
const pool = mysql.createPool({
  host: '127.0.0.1',
  user: 'root',
  password: 'cpm041019',  // ← 修改为您的MySQL密码
  database: 'ems',
  charset: 'utf8mb4',
  waitForConnections: true,
  connectionLimit: 10,
  timezone: '+08:00'
});
```

**请将 `password` 修改为您自己的 MySQL root 密码。**

---

## 四、安装项目依赖

1. 打开命令提示符，进入项目目录：
   ```bash
   cd D:\员工管理项目
   ```

2. 进入 server 目录：
   ```bash
   cd server
   ```

3. 安装 Node.js 依赖：
   ```bash
   npm install
   ```

---

## 五、启动项目

### 方法1：使用批处理文件启动（推荐）

双击运行项目根目录下的 `启动后端服务.bat`

### 方法2：使用命令行启动

1. 打开命令提示符，进入 server 目录：
   ```bash
   cd D:\员工管理项目\server
   ```

2. 启动后端服务：
   ```bash
   node server.js
   ```

3. 看到以下提示表示启动成功：
   ```
   EMS API 运行中: http://localhost:3001
   ```

---

## 六、访问系统

1. 后端服务启动后，用浏览器打开：
   ```
   D:\员工管理项目\login.html
   ```

2. 使用以下账号登录：

   | 账号类型 | 账号 | 密码 |
   |---------|------|------|
   | 系统管理员 | root | 123456 |
   | 普通员工 | （查看数据库中的员工账号） | admin |

3. 查看员工账号的方法：
   ```sql
   USE ems;
   SELECT username, name, password FROM employees LIMIT 10;
   ```

---

## 七、常见问题

### 问题1：导入时提示 "Access denied"

**原因**：MySQL 密码错误

**解决**：
1. 确认 MySQL root 密码是否正确
2. 如果忘记密码，需要重置 MySQL root 密码

---

### 问题2：导入时提示 "Unknown database"

**原因**：数据库不存在

**解决**：
1. 先登录 MySQL：`mysql -u root -p`
2. 创建数据库：`CREATE DATABASE ems CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;`
3. 退出后重新导入

---

### 问题3：启动后端服务时提示 "Cannot find module"

**原因**：Node.js 依赖未安装

**解决**：
```bash
cd server
npm install
```

---

### 问题4：前端无法连接后端

**原因**：后端服务未启动或端口被占用

**解决**：
1. 确认后端服务已启动（看到 "EMS API 运行中" 提示）
2. 检查端口 3001 是否被占用：
   ```bash
   netstat -ano | findstr :3001
   ```
3. 如果被占用，修改 `server/server.js` 最后一行的端口号

---

### 问题5：登录时提示 "无法连接服务器"

**原因**：后端服务未启动或数据库连接失败

**解决**：
1. 确认后端服务已启动
2. 检查 `server/server.js` 中的数据库密码是否正确
3. 确认 MySQL 服务是否正在运行：
   ```bash
   net start | findstr MySQL
   ```

---

## 八、数据库表结构说明

导入后的数据库包含以下表：

| 表名 | 说明 | 主要字段 |
|-----|------|---------|
| **employees** | 员工表 | emp_id, username, password, name, gender, phone, position, department_id, role_id |
| **departments** | 部门表 | id, name |
| **roles** | 角色表 | id, name |
| **attendance** | 考勤表 | id, emp_id, check_date, status, check_in, check_out |
| **leave_records** | 请假记录表 | id, emp_id, leave_type, start_date, end_date, days, reason, status |

---

## 九、初始数据说明

数据库导入后会包含以下初始数据：

### 部门（departments）
- 技术部
- 产品部
- 市场部
- 人力资源部
- 财务部
- 行政部

### 角色（roles）
- 管理员
- 部门主管
- HR专员
- 招聘专员
- 普通员工

### 员工（employees）
- 根据实际导出的数据而定
- 所有员工初始密码均为：`admin`

---

## 十、技术支持

如果遇到其他问题，请检查：

1. MySQL 版本是否为 8.0 或更高
2. Node.js 版本是否为 16.0 或更高
3. 是否正确修改了数据库密码
4. 防火墙是否阻止了端口 3001

---

## 十一、项目文件清单

确保您收到了以下文件：

```
员工管理项目/
├── login.html              # 登录页面
├── index.html              # 主界面
├── auth.js                 # 认证模块
├── main.js                 # 路由权限模块
├── app.js                  # 业务逻辑模块
├── style.css               # 样式文件
├── README.md               # 项目说明
├── 启动后端服务.bat         # 后端启动脚本
├── ems_database.sql        # 数据库导出文件 ← 重要！
├── 导入数据库说明.md        # 本文件
└── server/                 # 后端目录
    ├── server.js           # 后端主文件
    ├── package.json        # 依赖配置
    └── package-lock.json   # 依赖锁定文件
```

---

**祝您使用愉快！**
