# 员工管理系统 - 项目交接清单

## 📦 交接内容

### 1. 项目文件
- ✅ 前端页面（login.html, index.html）
- ✅ JavaScript 模块（auth.js, main.js, app.js）
- ✅ 样式文件（style.css）
- ✅ 后端服务（server/server.js）
- ✅ 依赖配置（server/package.json）
- ✅ 数据库文件（ems_database.sql）
- ✅ 启动脚本（启动后端服务.bat）
- ✅ 文档说明（README.md, 导入数据库说明.md）

### 2. 数据库
- ✅ 数据库导出文件：`ems_database.sql`
- ✅ 包含完整表结构和数据

---

## 🚀 快速开始指南（给同学）

### 第一步：导出数据库

**您需要做的：**

1. 双击运行 `导出数据库.bat`
2. 等待导出完成
3. 会在项目根目录生成 `ems_database.sql` 文件

---

### 第二步：打包项目文件

**方式1：压缩整个项目文件夹**

1. 右键点击 `员工管理项目` 文件夹
2. 选择"发送到" → "压缩(zipped)文件夹"
3. 生成 `员工管理项目.zip`

**方式2：使用 Git（如果项目在 Git 仓库中）**

```bash
# 初始化 Git 仓库（如果还没有）
git init
git add .
git commit -m "员工管理系统完整版"

# 推送到 GitHub/Gitee
git remote add origin <仓库地址>
git push -u origin main
```

---

### 第三步：发送给同学

**发送以下文件：**

1. ✅ `员工管理项目.zip`（或 Git 仓库链接）
2. ✅ `导入数据库说明.md`（已包含在项目中）

**通过以下方式发送：**
- 微信/QQ 文件传输
- 网盘分享（百度网盘、阿里云盘等）
- U盘拷贝
- Git 仓库（GitHub、Gitee）

---

## 📝 同学需要做的事情

### 1. 解压项目文件
```
解压 员工管理项目.zip 到任意目录
例如：D:\员工管理项目
```

### 2. 安装必要软件

**必须安装：**
- MySQL 8.0 或更高版本
- Node.js 16.0 或更高版本

**下载地址：**
- MySQL: https://dev.mysql.com/downloads/mysql/
- Node.js: https://nodejs.org/

### 3. 导入数据库

**方法1：使用命令行（推荐）**
```bash
# 登录 MySQL
mysql -u root -p

# 导入数据库（在 MySQL 命令行中执行）
source D:/员工管理项目/ems_database.sql

# 验证导入
USE ems;
SHOW TABLES;
```

**方法2：使用 MySQL Workbench**
1. 打开 MySQL Workbench
2. Server → Data Import
3. 选择 `ems_database.sql` 文件
4. 点击 Start Import

### 4. 修改数据库密码

打开 `server/server.js`，修改第13行：

```javascript
password: 'cpm041019',  // ← 改为自己的 MySQL 密码
```

### 5. 安装依赖

```bash
cd D:\员工管理项目\server
npm install
```

### 6. 启动后端服务

**方法1：双击运行**
```
双击 启动后端服务.bat
```

**方法2：命令行运行**
```bash
cd D:\员工管理项目\server
node server.js
```

看到以下提示表示成功：
```
EMS API 运行中: http://localhost:3001
```

### 7. 打开前端页面

用浏览器打开：
```
D:\员工管理项目\login.html
```

### 8. 登录系统

**管理员账号：**
- 账号：`root`
- 密码：`123456`

**员工账号：**
- 账号：查看数据库中的 `employees` 表
- 密码：`admin`（所有员工初始密码）

---

## ⚠️ 重要提醒

### 给您（项目发送方）

1. ✅ **务必先运行 `导出数据库.bat` 导出数据库**
2. ✅ **确保 `ems_database.sql` 文件已生成**
3. ✅ **检查项目文件是否完整**
4. ✅ **确认 `导入数据库说明.md` 已包含在项目中**

### 给同学（项目接收方）

1. ⚠️ **必须先导入数据库，再启动后端**
2. ⚠️ **必须修改 `server/server.js` 中的数据库密码**
3. ⚠️ **必须先启动后端，再打开前端页面**
4. ⚠️ **如果遇到问题，请查看 `导入数据库说明.md` 的常见问题部分**

---

## 🔧 常见问题速查

| 问题 | 原因 | 解决方法 |
|-----|------|---------|
| 导入数据库失败 | MySQL 密码错误 | 确认 root 密码是否正确 |
| 后端启动失败 | 依赖未安装 | 运行 `npm install` |
| 前端无法连接后端 | 后端未启动 | 先启动后端服务 |
| 登录提示"无法连接服务器" | 数据库密码错误 | 修改 `server/server.js` 中的密码 |
| 端口 3001 被占用 | 其他程序占用 | 修改 `server/server.js` 最后一行的端口号 |

---

## 📞 技术支持

如果同学遇到问题，可以：

1. 查看 `导入数据库说明.md` 的详细步骤
2. 检查 MySQL 和 Node.js 是否正确安装
3. 确认数据库密码是否正确修改
4. 查看后端控制台的错误信息

---

## ✅ 交接检查清单

**发送前请确认：**

- [ ] 已运行 `导出数据库.bat`
- [ ] `ems_database.sql` 文件已生成
- [ ] 项目文件已打包成 zip
- [ ] `导入数据库说明.md` 已包含在项目中
- [ ] 已告知同学需要安装 MySQL 和 Node.js
- [ ] 已告知同学需要修改数据库密码

**同学收到后请确认：**

- [ ] 已解压项目文件
- [ ] 已安装 MySQL 8.0+
- [ ] 已安装 Node.js 16.0+
- [ ] 已导入数据库
- [ ] 已修改数据库密码
- [ ] 已安装依赖（npm install）
- [ ] 后端服务已启动
- [ ] 前端页面可以打开
- [ ] 可以成功登录系统

---

**祝交接顺利！**
